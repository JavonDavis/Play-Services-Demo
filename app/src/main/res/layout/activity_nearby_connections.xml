<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.javon.playservicesdemo.nearbymessaging.NearbyConnectionsActivity">

    <Button
        android:text="Discover"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/discover_button"
        android:layout_above="@+id/advertise_button"
        android:layout_centerHorizontal="true"
        android:onClick="discover"
        android:layout_marginBottom="10dp" />

    <Button
        android:text="Advertise"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/advertise_button"
        android:onClick="advertise"
        android:layout_marginBottom="194dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/payload_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/discover_button"
        android:layout_below="@+id/discover_button"
        android:layout_marginTop="66dp"
        android:onClick="sendPayload"
        android:text="Send Message" />

</RelativeLayout>
